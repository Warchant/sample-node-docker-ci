machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t warchantua/sample-node .

test:
  override:
    - docker run warchantua/sample-node npm test

deployment:
  production:
    branch: master
    commands:
      - docker logic -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - chmod +x deploy.sh
      - sh ./deploy.sh
